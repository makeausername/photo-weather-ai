# 逐光天气设计系统

本文件定义 `photo-weather-ai` 项目的产品视觉、交互和信息架构基线。后续公开端、预测结果页和后台控制台的 UI 修改都应先对照本文件，避免回到居中演示页、通用蓝白 SaaS 模板或不适合中文风光摄影用户的界面方向。

## 1. 产品身份

- 产品名称：逐光天气。
- 公开标语：风光摄影出行判断工具。
- 产品类别：面向中国大陆风光摄影用户的天气与拍摄决策工具。
- 产品语气：专业、干净、冷静、实用、可信。
- 文案默认使用简体中文。技术 key、provider code、接口字段和内部包名可以保留英文。

## 2. 核心 UX 原则

逐光天气是一个决策工作台，不是居中的营销落地页。

核心流程应围绕用户是否值得出门拍摄：

1. 选择地点。
2. 选择预报范围。
3. 选择拍摄目标。
4. 查看天气、地图、图层和地点上下文。
5. 阅读决策摘要、时间窗口、风险和数据来源。
6. 决定是否出发拍摄。

界面优先呈现可操作判断：地点、时间、目标、天气窗口、风险、数据可信度和下一步动作。不要把首屏做成只有口号、按钮和留白的展示页面。

## 3. 公开端布局方向

桌面首页必须采用工具式布局：

- 左侧：查询 / 控制面板，包括地点搜索、预报范围、拍摄目标和开始分析。
- 中间：地图 / 天气 / 图层工作区，用于承载位置、云层、时间线和图层状态。
- 右侧：决策摘要面板，展示综合判断、关键窗口、风险和数据状态。
- 下方：场景模块、热门机位、工作流说明。

明确禁止：

- 窄居中的 hero 页面。
- 巨大的居中标题作为主要首屏。
- 所有内容挤在页面中线附近。
- 过大的左右空白边距导致桌面视口利用率低。
- 泛蓝白 SaaS 模板感。
- 与天气、地图、摄影无关的装饰性插画或空洞营销卡片。

页面应有足够的信息密度，但保持清晰扫描路径。面向风光摄影决策的工具区应成为首屏核心。

## 4. 响应式规则

- 桌面 `>=1200px`：优先使用三列工作区，左查询、中心地图/天气、右决策摘要。
- 平板 `900px-1199px`：使用两列或堆叠混合布局，常见组合为左查询 + 右侧工作区/摘要堆叠。
- 移动端 `<900px`：单列布局，查询、地图/天气、摘要、详情依次排列。
- 不允许横向溢出。
- 表格、时间线和卡片组在移动端必须可读、可滚动或改为单列。
- 表单控件、按钮、分段控件和地图/图层操作在移动端必须保持可点击，目标区域不应过小。
- 不用 viewport width 直接缩放文字；通过断点、容器宽度、换行和布局调整解决空间问题。

## 5. 色彩系统

默认使用自然摄影 / 天气工具色系：

| Token            | Hex       | 用途                         |
| ---------------- | --------- | ---------------------------- |
| `background`     | `#F7F4EC` | 页面背景                     |
| `surface`        | `#FFFDF7` | 面板、卡片、表单背景         |
| `surface-muted`  | `#ECE7DC` | 次级面板、输入底、浅背景区块 |
| `text-primary`   | `#17231F` | 主文本                       |
| `text-secondary` | `#66736D` | 辅助文本                     |
| `primary`        | `#2F6F5E` | 主操作、选中态               |
| `primary-hover`  | `#1F4F43` | 主操作 hover                 |
| `accent`         | `#D88A20` | 时间窗口、日出日落、重点提示 |
| `danger`         | `#B9472D` | 真实风险、错误和危险动作     |
| `info`           | `#5F8D8A` | 信息提示、低强度数据状态     |
| `border`         | `#DDD4C4` | 边框和分隔线                 |
| `ring`           | `#A9C7B8` | focus ring、交互高亮         |

规则：

- 固定使用亮色主题，不提供变体选择。
- 避免以蓝色作为主导色。蓝色只能少量用于外部链接或明确的信息语义，不应成为产品主视觉。
- 琥珀色只用于时间、日出日落、窗口机会和轻度提醒。
- 红色只用于真实风险、错误、删除或危险状态。
- 卡片、表格和工作区之间用背景层次与边框区分，不用大面积高饱和渐变。

## 6. 字体与排版

建议字号范围：

- 桌面 H1：`32-40px`。
- 页面标题：`24-28px`。
- 面板标题：`18-22px`。
- 卡片标题：`16-18px`。
- 正文：`15-16px`。
- 辅助说明：`12-14px`，必须保持可读。

规则：

- 避免巨型加粗营销标题。
- 中文正文需要舒适行高，通常 `1.65-1.8`。
- 仪表盘、表格、表单和侧栏内的标题应克制，不使用 hero 级字号。
- 不使用负 letter spacing。
- 长地点名、经纬度、provider 名称和状态说明必须可换行，不得撑破容器。

## 7. 组件规则

### 按钮

- 按钮应紧凑、清晰、可点击。
- 主按钮使用绿色 `primary`。
- 次按钮使用中性背景和边框。
- 危险按钮只用于删除、停用、撤销等真实风险动作。
- 不使用过大的蓝色块状按钮。
- 同一操作区内主按钮最多一个，避免多个同权重 CTA。

### 输入

- 输入框使用 `surface` 或 `surface-muted` 背景、清晰边框和可见 focus ring。
- 搜索、地点选择、预报范围和拍摄目标是核心控件，应优先靠近用户工作流。
- 输入错误必须给出中文原因和可恢复路径。

### 卡片

- 卡片用于承载独立信息单元，不要把整页区块都做成浮动卡片。
- 避免卡片套卡片。
- 圆角保持克制，通常不超过 `8px`，除非现有组件约定不同。
- 卡片内容应有明确标题、核心值和辅助说明。

### 徽章

- 徽章用于状态、来源、风险等级和 mock/真实数据区分。
- 文案必须是中文可理解标签，不直接暴露 raw enum。
- 数据源徽章必须诚实显示：模拟、夹具、真实服务商或本地算法。

### 表格

- 后台表格应紧凑、可扫描。
- 列标题中文明确，操作列稳定靠右。
- 移动端允许横向滚动表格，但不得让整页横向溢出。
- 空状态应说明当前没有数据，不使用夸张插画。

### Tabs / 分段控件

- 预报范围、拍摄目标、服务商类别等互斥选择优先使用分段控件或 tabs。
- 选中态使用 `primary` 或浅绿色背景，不使用蓝色胶囊。
- 控件应支持键盘 focus 和移动端换行。

### 警告横幅

- 模拟数据、配置缺失、坐标缺失、真实风险使用横幅或内联提示。
- 轻提醒用 `accent` 或 `info`，真实错误 / 危险用 `danger`。
- 警告必须说明影响和下一步，不只显示“错误”。

### 决策卡

- 决策卡的核心是“是否值得去拍”和“为什么”。
- 必须展示综合指数、推荐等级、最佳窗口、主要风险和数据状态。
- 避免夸张承诺，不应把模拟天气包装成真实预报。

### 时间线卡

- 时间线用于日出、日落、云海、星空和银河窗口。
- 使用紧凑时间、窗口标签、评分或风险状态。
- 琥珀色可用于日出日落窗口，高风险用红色。

### 数据源徽章

- 天文数据：本地算法计算。
- 天气数据：模拟数据、夹具数据或真实 provider 名称。
- 地形数据：模拟数据或真实 DEM / elevation provider 名称。

## 8. 后台 UI 规则

后台应是干净的 SaaS 控制台，不是公开营销页。

- 默认亮色主题。
- 侧栏紧凑，导航分组清晰。
- 顶部 header 紧凑，只显示当前页面、必要状态和账号控制。
- 只保留一个“返回前台”入口。
- 账号 / 用户控制应紧凑，不重复公开端入口。
- 不要同时出现多个“登录”“管理后台”“返回前台”等重复动作。
- 表格、表单和配置项密度应适合运营管理。
- 服务商密钥字段必须清晰、安全：默认只显示掩码、状态和更新入口；除非明确实现安全 reveal 流程，不展示原始 secret。
- 服务商配置页应区分启用状态、真实调用开关、mock/fixture/real 模式、配置缺失和测试结果。
- 后台错误提示需要说明是权限、配置、网络、校验还是服务商不可用。

## 9. 导航规则

公开导航使用以下主项：

- 首页
- 云海
- 朝霞晚霞
- 星空银河
- 机位库
- 定价
- 账户
- 开始分析

规则：

- 顶层公开导航不单独展示“登录”和“管理后台”作为并列主入口。
- 未登录用户点击“账户”可进入登录 / 账户占位流程。
- 已登录用户点击“账户”进入账户中心。
- 只有具备管理员权限的用户，才应在账户菜单或账户中心看到“管理后台”。
- “开始分析”是公开端主要动作，优先回到地点选择 / 分析工作区。

## 10. 数据诚实规则

界面必须清楚区分：

- 天文数据：本地算法计算。
- 天气数据：模拟数据或真实 provider 名称。
- 地形数据：模拟数据或真实 DEM / elevation provider 名称。

不得暗示 mock 数据是真实预报。所有模拟天气、模拟地形和夹具 provider 的结果都必须明确提示只用于流程验证或本地演示。真实 provider 接入前，不使用“实时”“精准预报”“已接入真实天气”等表达。

## 11. 天气与天文界面规则

Forecast 结果页应采用 dashboard 布局：

- 左侧：地点、查询条件、坐标、预报范围和分析目标。
- 中间：综合评分、推荐等级、时间窗口、分项评分和详情卡片。
- 右侧：风险、建议、计算依据和数据状态。

结果页必须展示：

- 预报起点。
- 预报终点。
- 覆盖日期。
- 时区。
- WGS84 经纬度。
- 坐标来源。
- 数据源状态。

天文信息展示规则：

- 日出 / 日落、太阳中天、晨光 / 昏影、月相、月出 / 月落、月亮照明、银河窗口应标注为本地算法计算。
- 银河窗口 V1 是初步估算，文案必须提醒仍需结合云量、月光、光污染和地形遮挡。
- 如果缺少有效 WGS84 坐标，必须阻止计算并提示：当前地点缺少有效 WGS84 坐标，无法计算日出日落、月相和银河窗口。
- 如果缺少有效预报时间范围，必须提示：缺少有效预报时间范围，无法生成拍摄天气分析。

## 12. Codex 指令

Before making UI changes, read this DESIGN.md and follow it. If a requested UI change conflicts with this document, prefer the product principles here unless the user explicitly overrides them.
